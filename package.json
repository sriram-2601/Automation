{
  "name": "agentflow-ai-root",
  "version": "1.0.0",
  "description": "Unified launcher for Agentflow_AI full-stack platform",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "start": "node server/src/index.js",
    "dev": "npx concurrently --kill-others \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
