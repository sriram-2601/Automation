{
  "name": "agentflow-ai-root",
  "version": "1.0.0",
  "description": "Unified launcher for Agentflow_AI full-stack platform",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "client": "npm run dev --prefix client",
    "server": "node server/src/index.js",
    "start": "npx concurrently --kill-others \"npm run dev --prefix client\" \"node server/src/index.js\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
